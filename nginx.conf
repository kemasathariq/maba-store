    server {
        listen 80 default_server;
        listen [::]:80 default_server;

        root /var/www/html/public; # Laravel public directory
        index index.php index.html index.htm;

        server_name _;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            include snippets/fastcgi-php.conf;
            fastcgi_pass unix:/var/run/php/php8.2-fpm.sock; # Adjust PHP version if needed
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include fastcgi_params;
        }

        location ~ /\.ht {
            deny all;
        }
    }

    ```

#### Langkah 2: Commit dan Push Perubahan

Simpan ketiga file baru ini (`Dockerfile`, `docker-entrypoint.sh`, `nginx.conf`), lalu commit dan push ke GitHub Anda.

```bash
git add Dockerfile docker-entrypoint.sh nginx.conf
git commit -m "feat: Add Docker configuration for Render deployment"
git push origin main
